const data = require('../data')

function get_product(product_id) {
    var pool = data.myData.getPool();

    var SQL_Select_Product = 'SELECT * FROM stock WHERE stock_code = ?'
    
    


    data.myData.getPool().query()
}