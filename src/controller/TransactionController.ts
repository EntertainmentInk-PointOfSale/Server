import { Request, Response, NextFunction, RequestHandler} from 'express';

export const postTransaction:RequestHandler = async (req:Request, res: Response, next:NextFunction) => {
    // POST transaction

    // Create new transaction item

    // Add sale items

    // Verify
}